/**
 * Database Seed Script
 * 
 * Usage:
 *   npx wrangler d1 execute rfil-db --remote --file=./apps/api/seed.sql
 * 
 * Or for local dev:
 *   npx wrangler d1 execute rfil-db --local --file=./apps/api/seed.sql
 */
console.log("Run: wrangler d1 execute rfil-db --remote --file=./apps/api/seed.sql");
